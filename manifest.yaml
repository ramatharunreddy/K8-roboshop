apiVersion: v1
kind: Pod
metadata: 
 name: nginx-pod
 namespace: roboshop-dev
 labels: 
  env: dev
spec: 
 containers:
 - name: nginx
   image: nginx
  # ports: 
  # - containerPort: 80
  resources:
   requests:
    cpu: "120m"
    memory: "250mi"
   limit:
    cpu: "500m"
    memory: "500mi"

